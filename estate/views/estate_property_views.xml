<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_estate_properties_tree" model="ir.ui.view">
        <field name="name">estate.properties.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Properties"
                  decoration-success="state in ('offer_received', 'offer_accepted')"
                  decoration-bf="state == 'offer_accepted'"
                  decoration-muted="state == 'sold'">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="property_tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" optional="hide"/>
                <field name="state" column_invisible="True"/>
            </tree>
        </field>
    </record>

    <record id="view_estate_properties_from" model="ir.ui.view">
        <field name="name">estate.properties.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Properties">
                <header>
                    <button name="action_sold" string="Sold" type="object" class="btn-primary"
                            invisible="state != 'offer_accepted'"/>
                    <button name="action_sold" string="Sold" type="object"
                            invisible="state in ('offer_accepted', 'sold', 'cancel')"/>
                    <button name="action_cancel" string="Cancel" type="object" invisible="state in ('sold', 'cancel')"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Title"/>
                        </h1>
                        <field name="property_tag_ids" widget="many2many_tags" placeholder="tags"
                               options="{'color_field': 'color'}"/>
                    </div>
                    <separator/>
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_offer"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description"/>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <field name="facades"/>
                                </group>
                                <group>
                                    <field name="garage"/>
                                    <field name="garden"/>
                                    <field name="garden_area" invisible="not garden"/>
                                    <field name="garden_orientation" invisible="not garden"/>
                                    <field name="total_area"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state not in ('new', 'offer_received')">
                                <tree editable="bottom"
                                      decoration-danger="state == 'refused'" decoration-success="state == 'accepted'">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="date_deadline"/>
                                    <button name="action_accept" type="object" icon="fa-check"
                                            invisible="state != False" title="Accept"/>
                                    <button name="action_refuse" type="object" icon="fa-times"
                                            invisible="state != False" title="Refuse"/>
                                    <field name="state" column_invisible="True"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="user_id" options="{'no_create': True}"/>
                                <field name="partner_id" options="{'no_create': True}"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_estate_properties_kanban" model="ir.ui.view">
        <field name="name">estate.properties.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban string="Properties" records_draggable="0" quick_create="False" default_group_by="property_type_id" group_create="False">
                <field name="name"/>
                <field name="expected_price"/>
                <field name="best_offer"/>
                <field name="selling_price"/>
                <field name="state"/>
                <field name="property_tag_ids"/>
                <template>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="name"/>
                                    </strong>
                                </div>
                                <div>Expected Price:
                                    <t t-esc="record.expected_price.value"/>
                                </div>
                                <div t-if="record.state.raw_value == 'offer_received'">Best Offer:
                                    <t t-esc="record.best_offer.value"/>
                                </div>
                                <div t-if="record.selling_price.raw_value">Selling Price:
                                    <t t-esc="record.selling_price.value"/>
                                </div>
                                <div>
                                    <field name="property_tag_ids" widget="many2many_tags"
                                           options="{'color_field': 'color'}"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </template>
            </kanban>
        </field>
    </record>

    <record id="view_estate_properties_search" model="ir.ui.view">
        <field name="name">estate.properties.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties">
                <field name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
                <group expand="1" string="Filter">
                    <filter string="Available" name="available" domain="[('state', 'in', ('new', 'offer_received'))]"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="search_view_id" ref="estate.view_estate_properties_search"/>
        <field name="context">{'search_default_available': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No records are available to show.
            </p>
        </field>
    </record>
</odoo>